This program demonstrate a simple software rasterization displayed via DirectX.
You can move the camera with WASD similar in fps games.
Rotate the camera when you pressed (and stay pressed!) the left mouse-button, and move the mouse everywhere inside the program`s window.
